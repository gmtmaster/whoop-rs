//! Heart-rate variability — ln-RMSSD and readiness scoring over R-R runs.
//!
//! TODO(batch): port the gap-aware RMSSD chain and HRV-readiness tiering; inputs are protocol-free R-R
//! runs (see `crate::sleep::RrRun`).
